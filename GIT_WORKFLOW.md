# 🔄 完整 Git 工作流程详解

## 📊 核心概念图

```
┌─────────────────────────────────────────────────────────────┐
│                    远程仓库（GitHub）                        │
│                  https://github.com/...                     │
└────────────────────────┬────────────────────────────────────┘
                         │
                    git push ↑
                    git pull ↓
                         │
┌────────────────────────▼────────────────────────────────────┐
│              本地仓库（.git 目录）                            │
│          存储所有提交历史和分支信息                           │
└────────────────────────┬────────────────────────────────────┘
                         │
                git commit ↑
                git reset ↓
                         │
┌────────────────────────▼────────────────────────────────────┐
│                   暂存区（Staging）                          │
│              git add 添加的文件在这里                        │
└────────────────────────┬────────────────────────────────────┘
                         │
                git add ↑
                git rm ↓
                         │
┌────────────────────────▼────────────────────────────────────┐
│              工作目录（Working Directory）                    │
│              你在电脑上修改的文件                            │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎯 四个关键区域

### 1️⃣ **工作目录（Working Directory）**
- 你在电脑上看到的实际文件
- 可以修改、添加、删除文件
- Git 追踪这些文件的变化

### 2️⃣ **暂存区（Staging Area / Index）**
- 中间层，用于选择性提交
- 使用 `git add` 将文件从工作目录移入
- 只有暂存区的文件才会被提交

### 3️⃣ **本地仓库（Local Repository）**
- 你电脑上的 `.git` 目录
- 存储所有提交的历史记录
- 包含所有分支信息

### 4️⃣ **远程仓库（Remote Repository）**
- GitHub、GitLab 等服务器上的仓库
- 与他人共享代码的地方
- 官方版本的代码

---

## 🔄 完整工作流程（一步步）

### 场景：修改一个文件并上传到 GitHub

#### 第一步：修改文件

```
你的电脑
    ↓
[打开 main.py 并修改]
    ↓
文件状态：已修改（Modified）
    ↓
工作目录
```

**此时的状态：**
```powershell
git status
# On branch main
# Changes not staged for commit:
#   modified: main.py
```

#### 第二步：查看改动（可选）

```powershell
# 查看文件有什么改动
git diff main.py

# 输出示例：
# - old_code = "hello"
# + new_code = "hello world"
```

#### 第三步：添加到暂存区

```
工作目录（修改后的 main.py）
    ↓
git add main.py
    ↓
暂存区（等待提交的文件）
```

**此时的状态：**
```powershell
git status
# On branch main
# Changes to be committed:
#   modified: main.py
```

#### 第四步：提交到本地仓库

```
暂存区（main.py）
    ↓
git commit -m "修复：更新 main.py 中的问题"
    ↓
本地仓库（保存提交历史）
```

**此时的状态：**
```powershell
git status
# On branch main
# nothing to commit, working tree clean

git log --oneline
# abc1234 (HEAD -> main) 修复：更新 main.py 中的问题
# def5678 Initial commit
```

#### 第五步：推送到 GitHub

```
本地仓库（main 分支）
    ↓
git push origin main
    ↓
远程仓库（GitHub）
```

**此时的状态：**
```powershell
git push origin main
# Enumerating objects: 3, done.
# Counting objects: 100% (3/3), done.
# Writing objects: 100% (2/2), 250 bytes | 250.00 KiB/s, done.
# Total 2 (delta 1), reused 0 (delta 0), reused pack 0
#    def5678..abc1234  main -> main

# GitHub 上已有最新版本了！
```

---

## 📋 常见操作速查

### 场景 1: 修改文件流程

```powershell
# 1. 修改文件（用编辑器打开）
# 编辑 main.py...

# 2. 查看改动
git diff main.py

# 3. 暂存改动
git add main.py

# 4. 提交改动
git commit -m "feat: 添加新功能"

# 5. 推送到 GitHub
git push

# 总结：编辑 → diff → add → commit → push
```

### 场景 2: 多个文件改动

```powershell
# 方式1：一次添加所有改动
git add .
git commit -m "多文件更新"
git push

# 方式2：分别添加
git add main.py
git add js/app.js
git commit -m "多文件更新"
git push

# 方式3：分次提交
git add main.py
git commit -m "更新后端"
git add js/app.js
git commit -m "更新前端"
git push
```

### 场景 3: 撤销改动

```powershell
# 还没 add，撤销工作目录的改动
git checkout -- main.py

# 已 add，但还没 commit，撤销暂存
git reset main.py

# 已 commit，撤销最后一次提交（保留改动）
git reset --soft HEAD~1

# 已 commit，撤销最后一次提交（不保留改动）
git reset --hard HEAD~1

# 已 push，需要撤销远程版本（危险！）
git revert HEAD
git push
```

### 场景 4: 从 GitHub 获取最新代码

```powershell
# 方式1：获取最新代码（自动合并）
git pull

# 方式2：分步操作
git fetch          # 获取远程代码
git merge origin/main  # 合并到本地

# 等价于：git pull = git fetch + git merge
```

---

## 🌳 分支工作流程

### 场景：开发新功能

```
main 分支（主分支，生产环境）
    │
    ├─ feature/new-feature（新功能分支）
    │      ├─ commit 1
    │      ├─ commit 2
    │      └─ commit 3
    │
    └─ 合并回 main
           └─ merge commit
```

**具体命令：**

```powershell
# 1. 创建并切换到新分支
git checkout -b feature/new-feature

# 2. 在新分支上开发
echo "新代码" >> main.py
git add .
git commit -m "feat: 实现新功能"

# 3. 推送分支到 GitHub
git push -u origin feature/new-feature

# 4. 在 GitHub 上创建 Pull Request（PR）
# 或者本地合并：
git checkout main
git merge feature/new-feature

# 5. 删除分支
git branch -d feature/new-feature
git push origin --delete feature/new-feature
```

---

## 🔍 查看历史和状态

### 查看提交历史

```powershell
# 简洁版
git log --oneline
# abc1234 修复：更新代码
# def5678 feat：新增功能
# ghi9012 Initial commit

# 完整版
git log
# commit abc1234567890abcdef1234567890abcdef123456
# Author: Your Name <your-email@example.com>
# Date:   Wed Nov 20 10:30:45 2025 +0800
#     修复：更新代码

# 图形化版本（含分支）
git log --graph --oneline --all
```

### 查看状态

```powershell
# 当前分支和文件状态
git status

# 简洁版本
git status -s
# M  main.py      (Modified)
# A  new_file.py  (Added)
# D  old_file.py  (Deleted)
```

### 查看改动内容

```powershell
# 工作目录 vs 暂存区
git diff

# 工作目录 vs 本地仓库
git diff HEAD

# 暂存区 vs 本地仓库
git diff --cached

# 两个提交之间的差异
git diff abc1234 def5678
```

---

## 🔐 初始化新仓库流程

### 从零开始（本地创建）

```powershell
# 1. 初始化本地仓库
cd my-project
git init

# 2. 配置用户信息
git config user.name "Your Name"
git config user.email "your-email@example.com"

# 3. 创建文件
echo "项目内容" > README.md

# 4. 第一次提交
git add .
git commit -m "Initial commit"

# 5. 连接远程仓库（GitHub 上已创建的空仓库）
git remote add origin https://github.com/username/repo.git
git branch -M main
git push -u origin main
```

### 克隆现有仓库

```powershell
# 1. 克隆远程仓库到本地
git clone https://github.com/username/repo.git

# 2. 进入项目目录
cd repo

# 3. 开始开发
# ... 修改文件 ...

# 4. 提交和推送
git add .
git commit -m "修改内容"
git push
```

---

## 🎓 工作流程对比

### 简单工作流（单人开发）

```
编辑 → add → commit → push
↓      ↓      ↓       ↓
工作   暂存   本地    远程
```

**命令：**
```powershell
# 编辑文件...
git add .
git commit -m "提交信息"
git push
```

### 团队工作流（多人开发）

```
fetch → merge → 编辑 → commit → push
  ↓      ↓       ↓      ↓       ↓
拉取   合并    修改   提交    推送
```

**命令：**
```powershell
# 1. 获取最新代码
git pull

# 2. 创建特性分支
git checkout -b feature/xxx

# 3. 编辑并提交
git add .
git commit -m "实现功能"

# 4. 推送到 GitHub
git push -u origin feature/xxx

# 5. 创建 Pull Request 请求合并
# 在 GitHub 上创建 PR，等待审查

# 6. 合并到 main
# （由其他人审查后合并）
```

---

## 📦 本地仓库 vs 远程仓库

### 本地仓库（.git 目录）

```
.git/
├── HEAD               # 指向当前分支
├── config             # 配置文件
├── objects/           # 存储所有提交对象
├── refs/
│   ├── heads/         # 本地分支
│   └── remotes/       # 远程分支
└── logs/              # 操作日志
```

### 远程仓库（GitHub）

```
GitHub Repository
├── main 分支
├── develop 分支
├── feature/xxx 分支
└── Pull Requests
    └── Issues
```

### 同步关系

```
本地 main ←→ 远程 origin/main
  ↑               ↓
  │ git push    git pull
  └──────────────┘
```

---

## ⚠️ 常见错误和解决方案

### 错误 1: 忘记 add 就 commit 了

```powershell
# 现象：修改了文件，但 commit 后发现没提交

# 解决：
git add .
git commit --amend
# （修改上次提交）
```

### 错误 2: 提交到了错误的分支

```powershell
# 现象：应该在 feature 分支，却在 main 分支上提交了

# 解决：
git reset --soft HEAD~1  # 撤销提交，保留改动
git checkout feature-branch
git commit -m "提交信息"
```

### 错误 3: 误删除了分支

```powershell
# 查看删除前的提交
git reflog

# 恢复分支
git checkout -b feature-branch abc1234
```

### 错误 4: 本地和远程不一致

```powershell
# 现象：push 失败，说远程有新内容

# 解决：
git pull  # 先拉取最新代码
git push  # 再推送
```

---

## 🎯 完整示例（从头到尾）

### 项目：添加一个新功能

```powershell
# ===== 第一天：开始开发 =====

# 1. 获取最新代码
git pull

# 2. 创建功能分支
git checkout -b feature/user-login

# 3. 编辑文件
# ... 打开编辑器，编写代码 ...

# 4. 查看改动
git status
git diff main.py

# 5. 暂存改动
git add main.py

# 6. 提交改动
git commit -m "feat: 实现用户登录功能"

# 7. 推送到 GitHub
git push -u origin feature/user-login

# ===== 第二天：继续开发 =====

# 1. 检查状态
git status

# 2. 继续编辑
# ... 编写更多代码 ...

# 3. 提交
git add .
git commit -m "feat: 添加密码验证"
git push

# ===== 完成：合并到 main =====

# 1. 在 GitHub 创建 Pull Request

# 2. 代码审查通过后，合并到 main

# 3. 本地同步
git checkout main
git pull
git branch -d feature/user-login
```

---

## 🚀 命令速查表

| 操作 | 命令 |
|------|------|
| **初始化** | `git init` |
| **查看状态** | `git status` |
| **查看改动** | `git diff` |
| **暂存文件** | `git add <file>` 或 `git add .` |
| **提交** | `git commit -m "信息"` |
| **推送** | `git push` 或 `git push origin main` |
| **拉取** | `git pull` |
| **查看历史** | `git log --oneline` |
| **创建分支** | `git checkout -b <branch>` |
| **切换分支** | `git checkout <branch>` |
| **删除分支** | `git branch -d <branch>` |
| **撤销改动** | `git checkout -- <file>` |
| **撤销 add** | `git reset <file>` |
| **撤销 commit** | `git reset --soft HEAD~1` |
| **查看分支** | `git branch -a` |
| **合并分支** | `git merge <branch>` |

---

## 📚 学习路径

### 初级（必会）
1. ✅ `git init` - 初始化仓库
2. ✅ `git add` - 暂存文件
3. ✅ `git commit` - 提交文件
4. ✅ `git push` - 上传到 GitHub
5. ✅ `git pull` - 从 GitHub 拉取 
6. ✅ `git status` - 查看状态
7. ✅ `git log` - 查看历史

### 中级（常用）
1. ✅ `git checkout` - 切换分支
2. ✅ `git branch` - 管理分支
3. ✅ `git merge` - 合并分支
4. ✅ `git diff` - 查看差异
5. ✅ `git reset` - 撤销提交
6. ✅ `git clone` - 克隆仓库

### 高级（进阶）
1. `git rebase` - 变基（重新应用提交）
2. `git stash` - 临时保存改动
3. `git cherry-pick` - 选择性合并提交
4. `git tag` - 标记版本
5. `git bisect` - 二分查找问题提交

---

## 💡 最佳实践

### ✅ 提交前检查

```powershell
# 1. 确保没有遗漏文件
git status

# 2. 查看即将提交的改动
git diff --cached

# 3. 提交清晰的信息
git commit -m "type: 简洁描述改动"
```

### ✅ 提交信息规范

```
feat:  新功能
fix:   修复bug
docs:  文档更新
style: 代码格式（无逻辑改动）
refactor: 代码重构
perf:  性能优化
test:  测试代码
chore: 构建/依赖/工具更新

示例：
git commit -m "feat: 添加用户登录功能"
git commit -m "fix: 修复登录页面错误"
git commit -m "docs: 更新 README"
```

### ✅ 定期推送

```powershell
# 不要堆积大量本地提交
# 频繁推送小提交更安全
git push  # 每完成一个小功能就推送
```

### ✅ 保持分支整洁

```powershell
# 及时删除已合并的分支
git branch -d feature/completed

# 定期清理远程已删除的分支
git fetch --prune
```

---

**现在你已经掌握了完整的 Git 工作流程！** 🎉

需要帮助时，查看本文档或运行 `git help <command>` 获取帮助。
